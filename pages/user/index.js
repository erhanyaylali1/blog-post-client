import Layout from '../../components/shared/Layout';
import UserContainer from '../../components/shared/UserContainer';

const User = () => {
  return (
    <Layout>
      <UserContainer>
        <div className="row m-0">User Profile</div>
      </UserContainer>
    </Layout>
  );
};

export default User;
